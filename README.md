# PixelOffice — 2D Multiplayer Metaverse

A real-time multiplayer virtual office built with Phaser, Colyseus, and React.

## Project Structure
- `/backend` — Colyseus game server
- `/frontend` — Phaser + React client

## Getting Started

### Backend
cd backend
npm install
npm run dev

### Frontend
cd frontend
npm install
npm run dev